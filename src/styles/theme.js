import styled from "styled-components";

const colors = {
  Main: "#17c4cf",
  Neutral: "#d9d9d9",
};

const common = {
  h1Title: `
    margin-bottom: 20px;
    color: #051929;
    font-size: 1.5rem;
    font-weight: 900;
    font-weight: 900;
  `,
};

const theme = {
  colors,
  common,
};

export default theme;
